/*global define, mxtest, $, console, window */
define(function (require) {
    'use strict';

    // Load library/vendor modules using
    // full IDs, like:
    var text = require('text'),
        template = require('text!app/components/wiringpi/template/wiringpi.html');

    function exec() {

        

    }

    return {
        execute: exec
    };
});